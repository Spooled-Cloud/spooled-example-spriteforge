apiVersion: v1
kind: Service
metadata:
  name: spooled-example-spriteforge
  labels:
    app.kubernetes.io/name: spooled-example-spriteforge
    app.kubernetes.io/component: example
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: spooled-example-spriteforge
